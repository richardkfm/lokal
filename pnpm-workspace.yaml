ignoredBuiltDependencies:
  - sharp
  - unrs-resolver

# better-sqlite3 ships a native addon that has to be built or downloaded at
# install time. Without this, the database driver loads but cannot connect.
onlyBuiltDependencies:
  - better-sqlite3
  - "@prisma/engines"
  - prisma
